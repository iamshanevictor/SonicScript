# Flask/SonicScript server environment variables
# These are optional and have sensible defaults in code.

# Folders
#UPLOAD_FOLDER=uploads
#PROCESSED_FOLDER=processed

# API base path (prefix for the Blueprint)
#API_BASE=/api

# Max upload size (bytes)
#MAX_CONTENT_LENGTH=33554432
